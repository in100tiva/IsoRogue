# Registro de Projetos

> Registro canônico exigido pela regra global do kit-mcp.
> Campos marcados com (obrigatório) não podem ficar vazios.

## Projeto principal: isorogue

- **Pasta local do projeto** (obrigatório): `/home/tech-lead/Documentos/DEV/isorogue`
- **Repositório do projeto** (obrigatório): https://github.com/in100tiva/IsoRogue
- **Documentação local** (obrigatório): `/home/tech-lead/Documentos/DEV/isorogue/obsidian`
- **Repositório da documentação** (opcional): —
- **Infra / VPS** (opcional): —
- **Notas** (opcional): o cofre Obsidian vive no próprio repositório do projeto; jogo 100%
  client-side, entregue como `dist/index.html` único. Roguelike isométrico por turnos —
  React 19 + TS + Vite; engine puro determinístico em `src/engine/` (intocável, R54), render
  canvas 2D em `src/render/`, UI React em `src/ui/`. Gate: `npm run check` (fronteiras +
  typecheck + 73 testes).

## Projetos conectados

<!-- Cada projeto adicional DEVE preencher todos os campos obrigatórios. -->
